name: testjon

on: 
  push: 
  
  workflow_dispatch: 

jobs:
  deploy-job:
   runs-on: ubuntu-latest
   needs: build-job
   steps:
     - name: 2ndf
       run: |
        echo "Raj"
        
  build-job:
   runs-on: ubuntu-latest
   container: 
    image: alpine
   steps: 
     - name: test
       run: |
        echo "Abhi"
        
     
    
     

